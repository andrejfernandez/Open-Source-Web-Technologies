// Style
import styled from "styled-components";

const Checkout = () => {
  return (
    <StyledCheckout>
      <h1>Checkout</h1>
    </StyledCheckout>
  );
};

const StyledCheckout = styled.div``;

export default Checkout;
